cars = ("Lamborgini", "Mercedes", "Toyota", "Honda")
x = list(cars)
x[3] = "BMW"
cars = tuple(x)
print(cars)
##You can convert the tuple into a list, change the list, and convert the list back into a tuple.

fruits = ("apple", "orange", "mango")
y = list(fruits)
y.append("kiwi")
fruits = tuple(y)
print(y)
thistuple = ("apple", "banana", "cherry")
y = ("orange",)
thistuple += y

print(thistuple)
