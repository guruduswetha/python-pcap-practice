class myclass: ##myclass is a class name. it defines what myclass object should look like##
    def __init__(self, age, name, birthyear): ## def means you are defining your function telling python to how to define.__init__ is a special, “magic” method name that Python recognizes as the constructor##
        self.age = age
        self.name = name
        self.birthyear = birthyear     
    def __str__(self):
        return f"{self.age} ({self.name}) {self.birthyear}" 
m1 = myclass( 34,"Swetha", 5)
print(m1)        
    
        
